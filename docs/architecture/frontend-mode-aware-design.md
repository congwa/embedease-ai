# 前端模式感知架构设计 (v2)

## 一、现有问题分析

### 1.1 核心痛点

| 问题 | 描述 | 影响 |
|------|------|------|
| **模式不可见** | 当前模式（单Agent/Supervisor）没有全局显示 | 用户不知道系统处于什么状态 |
| **编辑入口分散** | Supervisor配置在设置中心，Agent配置在Agent管理 | 操作割裂，流程不连贯 |
| **上下文切换频繁** | 配置Supervisor需跳转多个页面 | 认知负担重，容易迷失 |
| **模式切换隐蔽** | 只能通过Quick Setup切换模式 | 日常调整不便 |
| **编辑器不贴合模式** | 单Agent和Supervisor模式下编辑界面相同 | 功能暴露不合理 |

### 1.2 用户场景分析

```
场景A：新用户首次配置
├── 期望：快速理解两种模式差异，选择适合的模式
├── 现状：Quick Setup 可以选择，但后续编辑不直观
└── 改进：模式选择后，自动进入对应的配置流程

场景B：单Agent用户日常管理
├── 期望：专注于单个Agent的配置，不被Supervisor干扰
├── 现状：侧边栏有Supervisor入口，造成困惑
└── 改进：单Agent模式下隐藏Supervisor相关入口

场景C：Supervisor用户管理多Agent
├── 期望：统一视图管理所有子Agent和路由策略
├── 现状：需要分别进入各个Agent页面配置
└── 改进：提供Supervisor控制台，一站式管理

场景D：模式切换
├── 期望：快速切换模式，保留已有配置
├── 现状：需要重新进入Quick Setup
└── 改进：设置中心提供一键切换
```

---

## 二、新架构设计

### 2.1 设计原则

1. **模式感知（Mode-Aware）**：所有页面根据当前模式调整 UI 和功能
2. **上下文一致（Context-Consistent）**：减少页面跳转，保持操作连贯性
3. **渐进披露（Progressive Disclosure）**：根据用户水平显示不同复杂度的选项
4. **状态透明（State Transparency）**：全局显示当前模式和激活的 Agent

### 2.2 全局模式状态

```typescript
// 全局模式状态
interface SystemMode {
  mode: 'single' | 'supervisor';
  activeAgentId: string | null;        // 单Agent模式下激活的Agent
  supervisorConfig: {
    enabled: boolean;
    defaultAgentId: string | null;
    subAgents: SubAgentConfig[];
  };
}

// 通过 Zustand Store 全局管理
const useSystemModeStore = create<SystemModeState>((set) => ({
  mode: 'single',
  activeAgentId: null,
  supervisorConfig: null,
  setMode: (mode) => set({ mode }),
  // ...
}));
```

---

## 三、页面结构重设计

### 3.1 新路由结构

```
/admin                          # 管理后台根路由
├── /                           # 仪表盘（模式感知）
├── /quick-setup               # 快速设置向导
│
├── /workspace                 # ⭐ 新增：工作空间（核心入口）
│   ├── /                      # 根据模式显示不同界面
│   ├── /agent/[id]           # 单Agent编辑器
│   └── /supervisor           # Supervisor编辑器
│
├── /agents                    # Agent 管理（列表视图）
│   ├── /                      # Agent 列表
│   └── /create               # 创建Agent向导
│
├── /skills                    # 技能管理
├── /crawler                   # 爬虫管理
├── /conversations             # 会话管理
├── /users                     # 用户管理
└── /settings                  # 设置中心
    ├── /                      # 系统配置
    └── /mode                  # 模式切换（新增）
```

### 3.2 工作空间 `/admin/workspace` (核心重设计)

**职责**：模式感知的统一编辑入口

#### 3.2.1 单Agent模式下的工作空间

```
┌─────────────────────────────────────────────────────────────────────┐
│  工作空间                                    [模式: 单Agent ▼]      │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  当前Agent: 商品推荐助手                    [切换 ▼] [编辑]  │   │
│  │  类型: product | 状态: 启用 | 会话: 1,234                   │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────┬─────────────────────────────────────────────────┐ │
│  │  导航       │  内容区域                                       │ │
│  │             │                                                 │ │
│  │  基础设置 ← │  [Agent 基础信息编辑]                          │ │
│  │  工具配置   │  - 名称、描述、系统提示词                      │ │
│  │  知识库     │  - 类型特定配置                                │ │
│  │  技能       │                                                 │ │
│  │  中间件     │                                                 │ │
│  │  欢迎语     │                                                 │ │
│  │  推荐问题   │                                                 │ │
│  │  ─────────  │                                                 │ │
│  │  测试对话   │                                                 │ │
│  │             │                                                 │ │
│  └─────────────┴─────────────────────────────────────────────────┘ │
│                                                                     │
│  💡 提示：当前为单Agent模式。如需多Agent协作，请切换到Supervisor模式 │
└─────────────────────────────────────────────────────────────────────┘
```

#### 3.2.2 Supervisor模式下的工作空间

```
┌─────────────────────────────────────────────────────────────────────┐
│  工作空间                                    [模式: Supervisor ▼]   │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  Supervisor 编排                                             │   │
│  │                                                              │   │
│  │     ┌─────────────┐                                          │   │
│  │     │ Supervisor  │ ← 路由策略、默认Agent                    │   │
│  │     └──────┬──────┘                                          │   │
│  │            │                                                 │   │
│  │  ┌─────────┼─────────┬─────────┐                            │   │
│  │  │         │         │         │                            │   │
│  │  ▼         ▼         ▼         ▼                            │   │
│  │ ┌───┐    ┌───┐    ┌───┐    ┌───┐                           │   │
│  │ │商品│    │FAQ│    │KB │    │ + │                           │   │
│  │ └───┘    └───┘    └───┘    └───┘                           │   │
│  │                                                              │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────┬─────────────────────────────────────────────────┐ │
│  │  导航       │  内容区域                                       │ │
│  │             │                                                 │ │
│  │  编排概览 ← │  [子Agent列表 + 路由配置]                      │ │
│  │  路由策略   │                                                 │ │
│  │  ─────────  │  ┌──────────────────────────────────────────┐  │ │
│  │  子Agent    │  │ #1 商品助手                              │  │ │
│  │  • 商品助手 │  │ 优先级: 100  关键词: 商品,推荐,价格      │  │ │
│  │  • FAQ助手  │  │ [编辑] [测试] [禁用]                     │  │ │
│  │  • KB助手   │  └──────────────────────────────────────────┘  │ │
│  │  ─────────  │                                                 │ │
│  │  调度提示词 │  ┌──────────────────────────────────────────┐  │ │
│  │  全局欢迎语 │  │ #2 FAQ助手                               │  │ │
│  │  ─────────  │  │ 优先级: 90   关键词: 问题,帮助           │  │ │
│  │  测试对话   │  │ [编辑] [测试] [禁用]                     │  │ │
│  │             │  └──────────────────────────────────────────┘  │ │
│  └─────────────┴─────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 四、模式切换机制

### 4.1 切换入口统一

```
切换模式的入口：
├── 顶部导航栏：[模式: 单Agent ▼] 下拉菜单
├── 设置中心：/admin/settings/mode 专门页面
└── Quick Setup：首次配置或重新配置

建议：顶部导航栏作为快捷入口，设置中心作为详细配置入口
```

### 4.2 模式切换流程

```
┌─────────────────────────────────────────────────────────────────┐
│                        模式切换确认                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  您即将从 [单Agent模式] 切换到 [Supervisor模式]                │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  影响说明：                                               │ │
│  │  ✓ 当前Agent将成为Supervisor的默认子Agent                 │ │
│  │  ✓ 您可以添加更多子Agent进行协作                          │ │
│  │  ✓ 用户请求将由Supervisor智能路由                         │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  配置迁移：                                               │ │
│  │  • 欢迎语 → 保留为全局欢迎语                              │ │
│  │  • Agent配置 → 保留为子Agent配置                          │ │
│  │  • 技能 → 可分配给各子Agent                               │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│                    [取消]  [确认切换]                           │
└─────────────────────────────────────────────────────────────────┘
```

### 4.3 模式状态指示器

```tsx
// 全局模式指示器组件
const ModeIndicator = () => {
  const { mode, activeAgentId } = useSystemModeStore();
  
  return (
    <DropdownMenu>
      <DropdownMenuTrigger asChild>
        <Button variant="outline" className="gap-2">
          {mode === 'supervisor' ? (
            <>
              <Network className="h-4 w-4" />
              <span>Supervisor 模式</span>
            </>
          ) : (
            <>
              <Bot className="h-4 w-4" />
              <span>单 Agent 模式</span>
            </>
          )}
          <ChevronDown className="h-4 w-4" />
        </Button>
      </DropdownMenuTrigger>
      <DropdownMenuContent align="end">
        <DropdownMenuItem onClick={() => switchMode('single')}>
          <Bot className="h-4 w-4 mr-2" />
          单 Agent 模式
          {mode === 'single' && <Check className="h-4 w-4 ml-auto" />}
        </DropdownMenuItem>
        <DropdownMenuItem onClick={() => switchMode('supervisor')}>
          <Network className="h-4 w-4 mr-2" />
          Supervisor 模式
          {mode === 'supervisor' && <Check className="h-4 w-4 ml-auto" />}
        </DropdownMenuItem>
        <DropdownMenuSeparator />
        <DropdownMenuItem onClick={() => router.push('/admin/settings/mode')}>
          <Settings className="h-4 w-4 mr-2" />
          模式设置
        </DropdownMenuItem>
      </DropdownMenuContent>
    </DropdownMenu>
  );
};
```

---

## 五、侧边栏动态菜单

### 5.1 模式感知侧边栏

```typescript
// 根据模式动态生成菜单
const getSidebarMenus = (mode: SystemMode['mode']) => {
  const baseMenus = [
    { label: '仪表盘', path: '/admin', icon: LayoutDashboard },
    { label: '工作空间', path: '/admin/workspace', icon: Briefcase, highlight: true },
  ];

  const systemMenus = [
    { label: 'Agent 管理', path: '/admin/agents', icon: Bot },
    { label: '技能管理', path: '/admin/skills', icon: Sparkles },
    { label: '会话管理', path: '/admin/conversations', icon: MessageSquare },
    { label: '用户管理', path: '/admin/users', icon: Users },
    { label: '设置中心', path: '/admin/settings', icon: Settings },
  ];

  // 单Agent模式：隐藏Supervisor相关入口
  // Supervisor模式：显示所有入口
  return [...baseMenus, ...systemMenus];
};
```

### 5.2 工作空间子导航

```typescript
// 工作空间内部导航（根据模式变化）
const getWorkspaceSubMenus = (mode: SystemMode['mode'], agentType?: string) => {
  if (mode === 'supervisor') {
    return [
      { label: '编排概览', path: '/admin/workspace', icon: Network },
      { label: '路由策略', path: '/admin/workspace/routing', icon: Route },
      { divider: true },
      { label: '子Agent管理', subItems: true }, // 动态渲染子Agent列表
      { divider: true },
      { label: '调度提示词', path: '/admin/workspace/dispatch-prompt', icon: FileText },
      { label: '全局欢迎语', path: '/admin/workspace/greeting', icon: MessageCircle },
      { divider: true },
      { label: '测试对话', path: '/admin/workspace/test', icon: PlayCircle },
    ];
  }

  // 单Agent模式 - 根据Agent类型动态显示
  const menus = [
    { label: '基础设置', path: '/admin/workspace/agent/[id]', icon: Settings },
    { label: '工具配置', path: '/admin/workspace/agent/[id]/tools', icon: Wrench },
  ];

  // 根据Agent类型添加特定菜单
  if (agentType === 'product') {
    menus.push({ label: '商品数据', path: '/admin/products', icon: Package });
  }
  if (agentType === 'faq' || agentType === 'kb') {
    menus.push({ label: '知识库', path: '/admin/workspace/agent/[id]/knowledge', icon: Database });
  }
  if (agentType === 'faq') {
    menus.push({ label: 'FAQ管理', path: '/admin/workspace/agent/[id]/faq', icon: HelpCircle });
  }

  menus.push(
    { divider: true },
    { label: '技能', path: '/admin/workspace/agent/[id]/skills', icon: Sparkles },
    { label: '中间件', path: '/admin/workspace/agent/[id]/middleware', icon: Layers },
    { label: '欢迎语', path: '/admin/workspace/agent/[id]/greeting', icon: MessageCircle },
    { label: '推荐问题', path: '/admin/workspace/agent/[id]/questions', icon: List },
    { divider: true },
    { label: '测试对话', path: '/admin/workspace/agent/[id]/test', icon: PlayCircle },
  );

  return menus;
};
```

---

## 六、仪表盘模式感知

### 6.1 单Agent模式仪表盘

```
┌─────────────────────────────────────────────────────────────────────┐
│  仪表盘                                      [模式: 单Agent ▼]      │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │  当前Agent                                        [进入工作空间] │ │
│  │  ┌────────────────────────────────────────────────────────┐  │ │
│  │  │  🤖 商品推荐助手                                       │  │ │
│  │  │  类型: product | 技能: 3 | 今日会话: 156              │  │ │
│  │  └────────────────────────────────────────────────────────┘  │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐  │
│  │   会话数     │ │   消息数     │ │   用户数     │ │  响应时间    │  │
│  │   1,234     │ │   5,678     │ │    890      │ │   1.2s      │  │
│  └─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘  │
│                                                                     │
│  ┌─────────────────────────────┐ ┌─────────────────────────────┐  │
│  │  近期会话                    │ │  热门问题                    │  │
│  │  ...                        │ │  ...                        │  │
│  └─────────────────────────────┘ └─────────────────────────────┘  │
│                                                                     │
│  💡 提示：考虑升级到 Supervisor 模式，让多个 Agent 协作处理复杂场景  │
└─────────────────────────────────────────────────────────────────────┘
```

### 6.2 Supervisor模式仪表盘

```
┌─────────────────────────────────────────────────────────────────────┐
│  仪表盘                                    [模式: Supervisor ▼]     │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │  Supervisor 状态                                  [进入工作空间] │ │
│  │                                                               │ │
│  │  ┌─────────────────────────────────────────────────────────┐ │ │
│  │  │              ┌──────────┐                               │ │ │
│  │  │              │Supervisor│                               │ │ │
│  │  │              └────┬─────┘                               │ │ │
│  │  │        ┌──────────┼──────────┐                         │ │ │
│  │  │        ▼          ▼          ▼                         │ │ │
│  │  │     ┌────┐     ┌────┐     ┌────┐                      │ │ │
│  │  │     │商品│     │FAQ │     │ KB │                      │ │ │
│  │  │     │45% │     │30% │     │25% │  ← 请求分发比例       │ │ │
│  │  │     └────┘     └────┘     └────┘                      │ │ │
│  │  └─────────────────────────────────────────────────────────┘ │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐  │
│  │   总会话     │ │  路由成功率  │ │  子Agent数   │ │  平均响应    │  │
│  │   2,345     │ │   98.5%     │ │     3       │ │   0.8s      │  │
│  └─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘  │
│                                                                     │
│  ┌─────────────────────────────┐ ┌─────────────────────────────┐  │
│  │  子Agent性能对比             │ │  路由决策日志                │  │
│  │  ...                        │ │  ...                        │  │
│  └─────────────────────────────┘ └─────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 七、编辑器模式感知

### 7.1 Agent编辑器（单Agent模式）

**特点**：专注于单个Agent的完整配置

```tsx
// /admin/workspace/agent/[id]/page.tsx
const AgentEditor = () => {
  const { mode } = useSystemModeStore();
  
  // 单Agent模式下，显示完整配置
  // Supervisor模式下，隐藏部分全局配置（由Supervisor统一管理）
  const showGlobalSettings = mode === 'single';
  
  return (
    <div className="grid grid-cols-12 gap-6">
      {/* 左侧导航 */}
      <nav className="col-span-3">
        <AgentEditorNav agentType={agent.type} showGlobalSettings={showGlobalSettings} />
      </nav>
      
      {/* 右侧内容 */}
      <main className="col-span-9">
        <Outlet />
      </main>
    </div>
  );
};
```

### 7.2 Supervisor编辑器

**特点**：统一管理所有子Agent和路由策略

```tsx
// /admin/workspace/supervisor/page.tsx
const SupervisorEditor = () => {
  return (
    <div className="space-y-6">
      {/* 可视化编排图 */}
      <SupervisorOrchestrationGraph />
      
      {/* 子Agent列表 */}
      <SubAgentList 
        onEdit={(id) => router.push(`/admin/workspace/agent/${id}`)}
        onAdd={() => setShowAddDialog(true)}
      />
      
      {/* 路由策略配置 */}
      <RoutingStrategyConfig />
      
      {/* 调度提示词 */}
      <DispatchPromptEditor />
    </div>
  );
};
```

### 7.3 子Agent快速编辑面板

**特点**：在Supervisor视图中快速编辑子Agent，无需跳转

```
┌─────────────────────────────────────────────────────────────────────┐
│  编辑子Agent: 商品推荐助手                              [×]         │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  [Supervisor配置] [Agent配置] [测试]                                │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │  Supervisor 相关配置                                          │ │
│  │                                                               │ │
│  │  优先级:  [100    ]  (数值越大优先级越高)                     │ │
│  │  关键词:  [商品, 推荐, 价格, 购买]                             │ │
│  │  描述:    [处理商品查询和推荐请求]                             │ │
│  │  状态:    [● 启用] [○ 禁用]                                   │ │
│  │                                                               │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  [完整编辑 →]  ← 跳转到Agent完整编辑页面                           │
│                                                                     │
│                                         [取消] [保存]              │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 八、Quick Setup 重设计

### 8.1 模式选择步骤增强

```
┌─────────────────────────────────────────────────────────────────────┐
│  选择运行模式                                    步骤 2/6          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────┐ ┌─────────────────────────────┐  │
│  │                             │ │                             │  │
│  │     🤖                      │ │     🌐                      │  │
│  │                             │ │                             │  │
│  │   单 Agent 模式             │ │   Supervisor 模式           │  │
│  │                             │ │                             │  │
│  │   一个Agent处理所有请求     │ │   多个Agent协作处理请求     │  │
│  │                             │ │                             │  │
│  │   适用场景：                 │ │   适用场景：                 │  │
│  │   • 单一业务场景            │ │   • 多业务线并行            │  │
│  │   • 快速上线                │ │   • 专业分工                │  │
│  │   • 简单配置                │ │   • 复杂场景                │  │
│  │                             │ │                             │  │
│  │   [选择]                    │ │   [选择]                    │  │
│  └─────────────────────────────┘ └─────────────────────────────┘  │
│                                                                     │
│  💡 不确定？建议从单Agent模式开始，后续可随时切换                   │
│                                                                     │
│                                                 [上一步] [下一步]   │
└─────────────────────────────────────────────────────────────────────┘
```

### 8.2 模式相关步骤动态调整

```typescript
// 根据模式动态生成步骤
const getSetupSteps = (mode: 'single' | 'supervisor') => {
  const commonSteps = [
    { id: 'welcome', component: WelcomeStep },
    { id: 'mode', component: ModeStep },
    { id: 'system', component: SystemStep },
    { id: 'models', component: ModelsStep },
  ];

  if (mode === 'single') {
    return [
      ...commonSteps,
      { id: 'agent-type', component: AgentTypeStep },
      { id: 'knowledge', component: KnowledgeStep },
      { id: 'greeting', component: GreetingStep },
      { id: 'channel', component: ChannelStep },
      { id: 'summary', component: SummarySingleStep },
    ];
  }

  // Supervisor模式
  return [
    ...commonSteps,
    { id: 'supervisor-config', component: SupervisorConfigStep },
    { id: 'sub-agents', component: SubAgentsStep },
    { id: 'routing', component: RoutingStep },
    { id: 'greeting', component: GreetingStep },
    { id: 'channel', component: ChannelStep },
    { id: 'summary', component: SummarySupervisorStep },
  ];
};
```

---

## 九、API 设计

### 9.1 模式相关端点

| 功能 | 方法 | 端点 | 说明 |
|------|------|------|------|
| 获取当前模式 | GET | `/api/v1/admin/system/mode` | 返回 `{ mode, activeAgentId, supervisorConfig }` |
| 切换模式 | POST | `/api/v1/admin/system/mode` | Body: `{ mode, migrateConfig: boolean }` |
| 获取模式切换预览 | GET | `/api/v1/admin/system/mode/preview?to={mode}` | 返回切换影响分析 |

### 9.2 Supervisor相关端点

| 功能 | 方法 | 端点 | 说明 |
|------|------|------|------|
| 获取Supervisor配置 | GET | `/api/v1/admin/supervisor` | 完整配置 |
| 更新Supervisor配置 | PATCH | `/api/v1/admin/supervisor` | 部分更新 |
| 获取路由统计 | GET | `/api/v1/admin/supervisor/routing-stats` | 路由决策统计 |
| 子Agent排序 | PUT | `/api/v1/admin/supervisor/sub-agents/order` | 调整优先级 |

---

## 十、数据流设计

### 10.1 模式状态数据流

```
┌─────────────────────────────────────────────────────────────────────┐
│                           数据流                                    │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────┐    ┌──────────────┐    ┌────────────────────────┐ │
│  │             │    │              │    │                        │ │
│  │  后端 API   │ ←→ │ Zustand Store │ ←→ │  UI Components        │ │
│  │             │    │              │    │                        │ │
│  └─────────────┘    └──────────────┘    └────────────────────────┘ │
│         │                  │                        │              │
│         │                  │                        │              │
│         ▼                  ▼                        ▼              │
│  ┌─────────────┐    ┌──────────────┐    ┌────────────────────────┐ │
│  │             │    │              │    │                        │ │
│  │  数据库     │    │ LocalStorage │    │  URL State             │ │
│  │  (持久化)   │    │  (快速恢复)  │    │  (可分享)              │ │
│  │             │    │              │    │                        │ │
│  └─────────────┘    └──────────────┘    └────────────────────────┘ │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 10.2 模式切换状态机

```
          ┌──────────────────────────────────────────────────┐
          │                                                  │
          ▼                                                  │
    ┌──────────┐  select_single   ┌───────────────┐         │
    │  初始    │ ───────────────→ │  单Agent模式   │ ────────┤
    │          │                  │               │         │ switch_to_supervisor
    └──────────┘                  └───────────────┘         │
          │                             ↑                   │
          │ select_supervisor           │ switch_to_single  │
          ▼                             │                   │
    ┌───────────────┐                   │                   │
    │ Supervisor模式 │ ─────────────────┴───────────────────┘
    │               │
    └───────────────┘
```

---

## 十一、实施路线图

### 阶段一：基础设施 (P0)

1. **创建模式状态 Store** - `useSystemModeStore`
2. **实现模式指示器组件** - 全局显示当前模式
3. **API 端点开发** - 模式获取/切换接口

### 阶段二：工作空间 (P0)

4. **创建工作空间路由** - `/admin/workspace`
5. **单Agent编辑器** - 重构现有Agent编辑页面
6. **Supervisor编辑器** - 新建Supervisor配置页面

### 阶段三：模式切换 (P1)

7. **模式切换对话框** - 确认与迁移提示
8. **设置中心模式页** - `/admin/settings/mode`
9. **Quick Setup 集成** - 模式选择步骤增强

### 阶段四：仪表盘 (P1)

10. **模式感知仪表盘** - 根据模式显示不同卡片
11. **Supervisor统计视图** - 路由分发、性能对比

### 阶段五：优化 (P2)

12. **子Agent快速编辑面板** - 减少页面跳转
13. **可视化编排图** - 拖拽调整子Agent顺序
14. **模式切换动画** - 平滑过渡体验

---

## 十二、组件清单

### 新增组件

| 组件 | 路径 | 职责 |
|------|------|------|
| `ModeIndicator` | `components/admin/mode-indicator.tsx` | 全局模式指示器 |
| `ModeSwitchDialog` | `components/admin/mode-switch-dialog.tsx` | 模式切换确认对话框 |
| `WorkspaceLayout` | `app/admin/workspace/layout.tsx` | 工作空间布局 |
| `SupervisorEditor` | `app/admin/workspace/supervisor/page.tsx` | Supervisor编辑器 |
| `AgentEditorNav` | `components/admin/agent-editor-nav.tsx` | Agent编辑器侧边导航 |
| `SubAgentQuickEdit` | `components/admin/sub-agent-quick-edit.tsx` | 子Agent快速编辑面板 |
| `OrchestrationGraph` | `components/admin/orchestration-graph.tsx` | 可视化编排图 |

### 修改组件

| 组件 | 修改内容 |
|------|----------|
| `AdminLayout` | 添加模式指示器，动态侧边栏 |
| `DashboardPage` | 模式感知卡片展示 |
| `QuickSetupPage` | 模式选择步骤增强 |
| `SettingsPage` | 添加模式设置入口 |

---

## 十三、总结

本设计通过以下核心改进解决现有问题：

1. **全局模式可见性** - 顶部导航栏始终显示当前模式
2. **统一工作空间** - 一个入口适配两种模式
3. **模式感知UI** - 根据模式动态调整菜单和内容
4. **平滑切换体验** - 配置迁移、影响预览
5. **减少上下文切换** - 子Agent快速编辑面板

**预期效果**：
- 用户始终清楚系统处于什么模式
- 编辑操作贴合当前模式
- 操作流程更加连贯
- 降低认知负担
